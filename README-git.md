git clone <repo-url>
cd <repo-folder>

git branch -a //see all local and remote branches
git checkout branchA //go to what branch you want to work on

//pushlarken otomatik çekitğin branche pushlayacak
git add .
git commit -m "Your message"
git push -u origin branchA